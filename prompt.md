# Quant 界面新增策略说明板块修改 Prompt

你现在要修改 `quant` 项目的 Streamlit 界面，在现有智能选股相关展示区域中新增一个**面向 A 股投资者的策略说明板块**，用于解释一套“接近实盘”的交易规则。

## 目标

把下面这套策略总结，做成 `quant` 项目界面中的一个独立板块，偏“策略说明 / 规则看板”，不是新增回测引擎，也不是新增后台计算任务。

这套策略的核心主题是：

**三阳回踩 + 催化强化**

即：
- 近期出现三阳的股票进入观察范围
- 回踩过程中量流失不大，说明不是纯弱化
- 如果有美盘板块映射催化或消息面催化，则提高买入推荐优先级

## 代码上下文

当前项目是 Streamlit 单文件主界面，主入口是：

- `/home/phl/vv/quant/app.py`

当前智能选股相关 UI 主要集中在：

- `with st.sidebar.expander("智能选股（Smart Picker）", expanded=False):`
- `render_background_results_fragment()` 片段函数

当前智能选股结果区域已经包含这些板块：

- 中国宏观康波摘要
- 康波周期板块轮动与资金流
- 板块资金流明细
- 板块轮动与下周期繁荣预测
- 最近几天出现建仓信号的股票
- 科学选股推荐（趋势+资金+事件）
- 5日线回踩+MACD多头推荐
- 当日建议建仓（主策略 + 右侧确认候选）

## 你要做的事

在主界面中新增一个**策略说明板块**，建议放在以下位置之一：

1. `5日线回踩+MACD多头推荐` 后面、`当日建议建仓` 前面
2. 或者作为智能选股区域中的一个独立说明区块，紧贴 `smart_pick / ma5 / today_entry` 相关展示

优先选方案 1，因为这套规则和 `5日线回踩`、`当日建仓` 的用户理解路径最接近。

## 板块定位

这个新增板块不是技术文档，也不是一大段难读的纯文本。

它应该满足：

- 面向 A 股投资者，语言直观、可读
- 面向产品使用者，帮助理解“系统为什么这样筛”
- 面向策略研究者，规则有结构、可验证
- 不造成页面过于拥挤

## UI 设计要求

请沿用当前项目的简洁 Streamlit 风格，不要引入新依赖，不要做花哨前端。

推荐结构：

- `st.subheader("三阳回踩 + 催化强化策略说明")`
- 一行 `st.caption(...)` 说明这是策略规则板块，不构成投资建议
- 用 `st.tabs(...)` 或 `st.expander(...)` 把信息分组

推荐 tab 结构如下：

1. `策略定位`
2. `入池规则`
3. `回踩确认`
4. `催化加分`
5. `执行与风控`
6. `评分与验证`

如果你认为 `tabs` 太宽，也可以用多个 `st.expander`，但整体必须比直接堆一长段 markdown 更易读。

## 内容要求

请把下面的规则整理成用户可读、结构清晰的界面内容：

### 1. 策略定位

需要表达清楚：

- 这是一个“强势股回踩 + 催化强化”的短线偏右侧策略
- 不是抄底策略
- 先找被资金做强的票，再等回踩确认，再用催化做优先级排序

### 2. 适用市场环境

请明确写出：

- 适合偏强或结构性强势市场
- 至少关注：上涨家数、成交额、热点持续性、板块是否站上关键均线
- 普跌、退潮、轮动太快时应谨慎或暂停

可以整理成“适合 / 不适合”两列或两组 bullet。

### 3. 股票池范围

需要包含：

- 剔除 ST、次新、成交额过低、流动性失真、明显庄股、重大风险票
- 优先主线板块、辨识度高、板块内强势、相对指数更强的票

### 4. 入池规则

需要把“三阳”定义写得更接近实盘：

- 最近 5 个交易日内出现连续 3 根阳线
- 三日累计涨幅在合理区间
- 至少有放量确认
- 收盘站上关键均线
- 相对板块具备强度

请强调：

- 三阳不是直接买入信号
- 只是进入候选池

### 5. 回踩确认

这是核心部分，必须写清楚：

- 回踩发生在三阳后 1 到 5 个交易日内
- 回撤幅度适中
- 不能有效跌破关键均线
- 回踩时量缩但不能失真
- 不接受放量砸盘或连续中阴破位

### 6. 催化加分

请拆成两类：

- 美盘映射催化
- 国内消息催化

需要强调：

- 催化是加分项，不是单独买入理由
- 技术结构决定“能不能进候选池”
- 催化决定“候选池里谁优先”

### 7. 买入触发

要体现“接近实盘”的执行感：

- 不追高开过猛
- 看开盘承接与分时确认
- 允许首笔仓位 + 确认后加仓
- 不是只要进候选池就买

### 8. 否决条件

请列出清晰的否决项，例如：

- 三阳出现在高位末端
- 回踩放量下跌
- 板块明显转弱
- 催化已被竞价透支
- 个股有明显公告风险

### 9. 卖出与风控

至少包括：

- 止损条件
- 止盈条件
- 失效退出条件

要强调：

- 没有退出规则，策略不完整

### 10. 打分与验证

请做成简洁的结构化说明：

- 结构强度
- 回踩质量
- 催化强度
- 执行条件

并补充：

- 适合做候选池排序
- 实盘前要看胜率、回撤、连续亏损、不同市场环境表现

## 呈现风格要求

请避免以下问题：

- 不要写成学术论文
- 不要写成一屏超长纯文字
- 不要让用户感觉这是开发备忘录
- 不要使用英文术语堆砌，必要时中英混排即可

请尽量做到：

- 标题短
- 每条规则一句话能读懂
- 有轻度层次感
- A 股用户看一眼知道这板块在讲“什么时候选、什么时候不选、什么时候买、什么时候撤”

## 实现建议

建议你把这个板块提炼成一个单独的小渲染函数，避免继续把 `app.py` 主流程写得更臃肿。

例如：

- `def render_three_bull_pullback_strategy_guide() -> None:`

然后在 `render_background_results_fragment()` 的智能选股区域中调用。

如果你觉得当前文件已经足够拥挤，也可以把该函数放在 `app.py` 合适位置，但不要引入新依赖，不要做大规模重构。

## 明确不做

这次修改**不要**做以下事情：

- 不新增后台任务
- 不新增数据库表
- 不实现新的量化打分引擎
- 不修改现有 `smart_pick / ma5_pullback / today_entry` 的核心计算逻辑
- 不引入新三方依赖

这次只做：

- 新增一个清晰、可读、贴近实盘理解的策略说明板块

## 验收标准

修改完成后，应满足：

1. 页面中能看到一个新的独立板块，标题明确表达“三阳回踩 + 催化强化策略说明”
2. 板块内容不是单段大文本，而是结构化展示
3. 内容覆盖：定位、环境、入池、回踩、催化、执行、风控、评分验证
4. 不影响现有智能选股、5日线回踩、当日建仓等已有功能
5. `python3 -m py_compile app.py` 能通过

## 交付时请说明

完成修改后，请输出：

- 改了哪些文件
- 新板块插入在页面哪个位置
- 为什么选择该展示结构
- 做了哪些简化
- 是否通过最小验证
