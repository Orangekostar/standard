# Implementation Evidence

The release evidence is bound to code commit
`be7fa3895f3013156aa0b9d5ea65f0706d0c252f`. The CLI smoke used an isolated
demo database and artifact root. The worker smoke used a second clean root and
completed all seven nodes with one run/as-of/data hash binding.

The evidence directory contains CLI, database, worker, source-fidelity, prompt,
and verification summaries; the full derived evaluation package; and reviewed
desktop/mobile screenshots. No raw database or private input is included.

Key counts: 3 schema migrations, 641 sync audits, 131 persisted feature rows,
42 immutable prediction rows, 13,461 labels, two paper accounts, 12 decisions,
and two valuations. Zero orders and fills is the computed result of the frozen
eligibility rules, not a placeholder success path.
