# Technical V2 Research Release

This release completes the runtime behind the frozen Technical V2 contract:
incremental audited synchronization, real stock and sector publications, the
fixed evaluation protocol, dual paper accounts, matured scoring, production
worker handlers, and a read-only Streamlit view.

Verification completed with 187 tests, scoped Ruff and compile checks, a clean
seven-node worker DAG, SQLite integrity/foreign-key checks, prompt compliance
audit, evaluation artifact hash verification, and desktop/mobile UI review.

The checked-in public evidence is `DEMO_ONLY`. Formula selection remains
`NO_ELIGIBLE_CANDIDATE`, historical Jev evidence is unavailable, and the final
test remains unopened. No credentials, raw market database, user holdings, paid
Jev calls, live broker connection, or real orders are included.
